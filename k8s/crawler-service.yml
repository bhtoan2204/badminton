apiVersion: v1
kind: Service
metadata:
  name: crawler
  labels:
    app: crawler
spec:
  selector:
    app: crawler
  ports:
    - protocol: TCP
      port: 8088
      targetPort: 8088
  type: ClusterIP
