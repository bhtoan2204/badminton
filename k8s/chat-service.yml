apiVersion: v1
kind: Service
metadata:
  name: chat-service
  labels:
    app: chat
spec:
  selector:
    app: chat
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
  type: ClusterIP