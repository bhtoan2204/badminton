apiVersion: v1
kind: Service
metadata:
  name: calendar
  labels:
    app: calendar
spec:
  selector:
    app: calendar
  ports:
    - protocol: TCP
      port: 8089
      targetPort: 8089
  type: ClusterIP